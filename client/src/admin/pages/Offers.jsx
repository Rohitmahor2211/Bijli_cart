import { useEffect, useState } from 'react'; import api from '../../api/axios';
export default function Offers() { const [items,setItems]=useState([]); const [error,setError]=useState(''); useEffect(()=>{api.get('/offers').then(r=>setItems(r.data?.data?.offers||[])).catch(e=>setError(e.response?.data?.message||'Unable to load offers.'));},[]); return <div className="space-y-5"><div><h1 className="text-2xl font-bold">Offers</h1><p className="text-slate-500 text-sm">Active and scheduled promotions.</p></div>{error&&<p className="text-red-600 text-sm">{error}</p>}<div className="bg-white border rounded-2xl divide-y">{items.length?items.map(o=><div key={o._id} className="p-4 flex justify-between gap-4"><div><p className="font-semibold">{o.name}</p><p className="text-sm text-slate-500">{o.code} · {o.type} · {o.value}</p></div><span>{o.isActive?'Active':'Inactive'}</span></div>):<p className="p-6 text-slate-500">No offers yet.</p>}</div></div>; }
