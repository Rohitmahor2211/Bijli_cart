import { useEffect, useState } from 'react';
import api from '../../api/axios';
export default function Inventory() { const [items, setItems] = useState([]); const [error, setError] = useState(''); useEffect(() => { api.get('/inventory').then(r => setItems(r.data?.data?.inventory || [])).catch(e => setError(e.response?.data?.message || 'Unable to load inventory.')); }, []); return <div className="space-y-5"><div><h1 className="text-2xl font-bold">Inventory</h1><p className="text-slate-500 text-sm">Current product stock.</p></div>{error && <p className="text-red-600 text-sm">{error}</p>}<div className="bg-white border rounded-2xl overflow-x-auto"><table className="w-full text-left"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">Product</th><th className="p-4">SKU</th><th className="p-4">Available</th><th className="p-4">Reserved</th><th className="p-4">Low-stock level</th></tr></thead><tbody>{items.length ? items.map(i => <tr className="border-t" key={i._id}><td className="p-4 font-medium">{i.productId?.name || 'Deleted product'}</td><td className="p-4">{i.productId?.sku || '—'}</td><td className="p-4">{i.availableStock}</td><td className="p-4">{i.reservedStock}</td><td className="p-4">{i.lowStockThreshold}</td></tr>) : <tr><td colSpan="5" className="p-6 text-slate-500">No inventory yet.</td></tr>}</tbody></table></div></div>; }
